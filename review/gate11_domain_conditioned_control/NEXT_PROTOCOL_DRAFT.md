# Next protocol status

No Gate-12 protocol is drafted for execution because the Gate-11 forensic
audit found a scientific-integrity concern in raw propagation persistence.

The primary synthesis is preserved as
`GATE11_POLICY_UTILITY_DOMAIN_MISMATCH`, but the full vocabulary-logit arrays
and hidden-state difference vectors required to independently recompute the
primitive finite-displacement metrics were not retained. Principal review is
required before any new diagnostic collection.

If a future protocol is authorized, it must freeze raw per-checkpoint
baseline/condition logits, hidden differences, token indices, and normalization
metadata before collection. Gate 11 did not measure an exact local pullback or
Fisher metric. A separately authorized exact directional
Pullback-Fisher/Jacobian diagnostic would require local JVPs, small-epsilon
quadratic validation, per-token Fisher energy, meaningful-versus-random
comparisons, and CRUXEval-versus-character-count contrasts.

Executed: **NO**.
