# external-semantic-v2

The parser recognizes exactly one complete-line `FINAL:` commitment. It permits heading/list/checkmark decoration, matching emphasis or inline-code wrappers, and one matching closing Markdown fence. It rejects substantive suffix text, multiple commitments, missing or empty commitments, arbitrary reasoning extraction, and truncation. Reference-type-aware evaluation remains the canonical deterministic Python-literal evaluator.
