# Gate 12 prospective protocol lock

Status: `FROZEN_PRE_GEOMETRY`. Exact forward-mode JVPs are measured at alpha=0. Historical semantic outcomes remain inaccessible until `GEOMETRY_FREEZE`. This is a one-dimensional sustained-control pullback, not a full pullback matrix.
