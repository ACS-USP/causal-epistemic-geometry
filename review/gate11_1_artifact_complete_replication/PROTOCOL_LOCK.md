# Gate 11.1 prospective protocol lock

The historical Gate 11 design is reused byte-for-byte. Only the fixed-sequence propagation runner changes: full vocabulary logits and complete hidden-difference vectors are persisted per item shard. No free generation or new semantic outcome is authorized. The historical Gate 11 result and forensic concern remain immutable.
