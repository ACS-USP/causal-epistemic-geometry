# Gate 9 adversarial premortem

Classification: `PREMORTEM_PASS`.

Gate 9 imports D75 exactly from the prospectively completed Gate 8 calibration.
There is no remaining controller, layer, sign, dose, parser, benchmark, or item
selection. The exact L27 vector is verified byte-for-byte and by its canonical
float hash before lock and execution.

The fresh allocator reconstructs all historical and reserved CRUXEval IDs,
including Gate 8 calibration items. It deterministically allocates exactly 100
items and records every remaining unseen ID as unallocated and untouched.

Four new Gaussian directions are frozen before outcomes, orthogonalized against
the meaningful vector and one another, and matched at layer, eta, duration,
scope, engine, and token cap. Semantic V3 remains condition-symmetric and keeps
commitment validity, semantic evaluability, and correctness separate. Invalid,
truncated, and model-runtime outcomes remain primary errors; only genuine
infrastructure incidents may be retried under the same logical key and seed.

The complete schedule uses two globally distinct `INDEPENDENT_PRIMARY` seeds per
item-condition, is interleaved outcome-independently, and is journaled with
append/flush/fsync. Resume skips completed logical keys and cannot add a third
draw. Collection monitoring is restricted to infrastructure and row integrity.

The canonical unbiased two-rollout G/C/D estimands and 10,000-resample
item-cluster bootstrap are frozen. Textual CAREFUL is descriptive and cannot
select D75. The exact CORE_QWEN environment and remote preflight are required.

No unresolved Class A or Class B issue remains. No Gate-9 model output exists.
