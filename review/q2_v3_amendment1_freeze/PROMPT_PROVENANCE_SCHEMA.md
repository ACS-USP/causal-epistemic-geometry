# Amendment-1 prompt provenance schema

Every record separately types item, purpose, template, schema, exact UTF-8 bytes, raw byte hash, reference, source artifact, and a provenance digest. Legacy hashes remain historical-only fields. An ambiguous `prompt_hash` field is forbidden in executable Amendment-1 manifests.
