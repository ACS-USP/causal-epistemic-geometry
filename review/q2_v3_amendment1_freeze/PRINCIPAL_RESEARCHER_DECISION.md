# Q2 V3 principal-researcher prompt decision

The canonical model-visible constructor for every Q2 V3 purpose is `gate7-cruxeval-semantic-task-prompt-v1`. The legacy prompt is historical provenance only and is not an executable alternative. This prospective, outcome-independent decision follows a clean zero-inference abort.
