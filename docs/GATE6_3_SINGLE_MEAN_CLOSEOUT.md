# Gate 6.3 Single-Mean Semantic Evaluation Closeout

Gate 6.3 completed as a development experiment with the frozen Qwen3-8B ×
CRUXEval semantic substrate. The 80-row architecture-matched random supplement
passed its manipulation gate, so the frozen 840-row evaluation was executed.

The primary classification is:

`GATE6_3_SINGLE_MEAN_DESTRUCTIVE`

The meaningful single-L27 controller exceeded the four-random null on the
point estimates of `G`, `C`, and `D`, and stayed within the accuracy tolerance.
It nevertheless failed the pre-registered validity guard: validity was 0.9083,
below the required 0.9250 relative to the 0.9750 baseline. It is therefore not
promoted to a useful complementarity or movement signal.

The complete forensic report, raw journal, estimands, bootstrap intervals,
environment provenance, and hashes are in
[`review/gate6_3_single_mean_semantic_evaluation/`](../review/gate6_3_single_mean_semantic_evaluation/).

No Q2, character-count replication, or confirmatory-holdout access occurred.
The RunPod is stopped and the next action is principal researcher review.
