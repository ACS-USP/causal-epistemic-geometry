# Gate 12.1 continuous-geometry engine closeout

Gate 12.1 closed as `GATE12_1_DERIVATIVE_ENGINE_NOT_QUALIFIED`.

On twelve synthetic, non-scientific token fixtures, FP32 full-sequence and
sequential KV-cache execution passed the frozen equivalence thresholds. Exact
forward and independent JVPs, JVP/VJP duality, Fisher/Hessian identity, and the
utility derivative identity also passed. The historical-BF16 bridge failed its
top-1 threshold, and the finite-difference/local-KL ladder supplied only two
consecutive passing scales rather than the required three.

No scientific item was processed, no historical semantic outcome was revealed,
and no free generation occurred. Gate 12 remains historically immutable; this
is numerical-engine qualification evidence only, not predictive pullback or
Fisher-geometry evidence.
