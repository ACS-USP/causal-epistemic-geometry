# Gate 10 cross-domain character-count closeout

Gate 10 is closed as `GATE10_NO_CROSS_DOMAIN_TRANSFER` on 200 fresh
`FRESH_PSEUDOWORD_LONG` items. The exact CRUXEval-derived paired-mean L27-plus
controller, fixed at D75 (`eta=9.637427952852196`), was transported without
adaptation into exact long character counting.

The instrument had adequate error opportunity: baseline accuracy was 0.8625,
repeated-baseline double fault was 0.045, nine items were wrong in both
baseline rollouts, and 191 were correct in at least one. The controller was
safe—commitment validity and semantic evaluability were both 0.9975, and
accuracy was 0.835—but it did not reorganize blind spots beyond sampling or
the four new architecture-matched random controls. Its G/C/D were
-0.01625/-0.012299/-0.025; rescue was 0.07625 versus damage 0.10375. The
opportunity-normalized G was -0.361111.

The task-specific textual CAREFUL reference also failed its frozen source
replication rule: it increased tokens but reduced accuracy from 0.8625 to
0.805, more than the allowed three percentage points. The activation
controller recovered 19.3% of the textual token increase, below the frozen
style-transfer threshold.

Collection completed all 2,800 frozen logical rows after the principal raised
the total hard ceiling to US$6.00. The exact resume preserved the first ten
rows and generated only missing keys. Total A40 runtime was 42,476 seconds and
estimated incremental GPU cost was US$5.1915. Both execution Pods are stopped.

The independent audit found 2,800 unique scheduled rows, exact independent
seeds, condition-symmetric parser results, exact integer oracles, no historical
generator collision, exact primary/audit metric agreement, and classification
agreement. Its classification is `GATE10_FORENSIC_CLEAN`.

This result bounds transfer of this fixed controller. It does not overturn the
fresh CRUXEval replication, establish Q2, access the confirmatory holdout, or
authorize a new controller search. A domain-conditioned control postmortem is
drafted only; Gate 11 was not executed.
